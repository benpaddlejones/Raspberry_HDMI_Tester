# Export image after custom stage
# This file is sourced by pi-gen to configure the image export

IMG_SUFFIX="-hdmi-tester"
if [ "${USE_QEMU}" = "1" ]; then
    export IMG_SUFFIX="${IMG_SUFFIX}-qemu"
fi
